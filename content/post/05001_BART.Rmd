---
title: 吹气球（BART）
author: 张亮
date: 2018-03-03
slug: 05001-bart
code: "05001"
tasknames: "仿真气球模拟范式"
categories: "文档"
---

```{r basic settings, include=FALSE}
NAME_CN <- "吹气球"
NAME_EN <- "BART"
CODE <- "05001"
SDK <- 300
VER <- "V4"
```

# 基本信息

* **显示名称**：`r NAME_CN`
* **英文名称**：`r NAME_EN`
* **题目编号**：`r CODE`
* **任务描述**：测查自控力，特别是控制冲动的能力。
* **测查能力**：自控力-控制冲动

# 交互界面

如下图，屏幕中央展示一个气球；下方有左右两个按钮，分别采用一个**形状**表示**打气**（左侧）和**获取奖励**（右侧）；按钮下方有上下两个计分板，分别用**刻度**表示累计获得的奖励（上）和最近一个未爆炸气球成功兑换的奖励（下）。用户可以通过触碰按钮（“移动APP”）或使用鼠标点击（“电脑WEB”）或使用键盘的左右键（“电脑WEB”，需要在练习时候提示）而做出选择。

```{r include-image, fig.cap="主界面", fig.align='center', echo=FALSE}
knitr::include_graphics("/images/BART/界面.png")
```

选择结果（注意：选择结果没有正确与错误之分）：

* 选择“打气”
: 气球的大小**增大**一定尺度并等待用户进一步选择，或者气球大小已经到了极限而**爆炸**。请注意，**不要**显示当前大小的气球可以兑换的奖励数目。

* 选择“获取奖励”
: 同时完成四个更新：气球消失；展示获得奖励分数：奖励分数 $=$ 打气次数 $\times$ 5；增加累计分数的**刻度**累加进新的分数；更新最近获得奖励分数。

# 详细说明

* 气球爆炸几率
    + 假设气球最大打气次数为 $N$ （参数配置），那么，用户第 $K$ 次打气时的气球爆炸的概率为 $\frac{1}{N - K + 1}$ 。例如：
        - 若气球最大打气次数为`5`。
        - 用户执行第1次打气时，则从[1, 2, 3, 4, 5]的数组中随机取一个数字：若取到1，则打气后气球爆炸；若未取到1，则气球增大，同时奖金池增大5。
        - 若用户执行第2次打气，则从[1, 2, 3, 4]的数组中随机取一个数字：若取到1，则打气后气球爆炸；若未取到1，则气球增大，同时奖金池增大5。
        - 依此类推。

# 配置参数

: 配置参数总表

| 参数类型 | 参数名称               | 类型   | 默认值 | 说明                                             |
|----------|------------------------|--------|--------|--------------------------------------------------|
| 刺激序列 | `quantity`             | `int`  | 30     | 气球总个数                                       |
| 刺激序列 | `maxBlow`              | `int`  | 64     | 单个气球最大打气次数                             |
| 操作反馈 | `feedbackCorrectError` | `bool` | 0      | 注意本任务没有正确错误之分，也就不需要反馈正误了 |
| 操作反馈 | `feedbackNoResponse`   | `bool` | 0      | 注意本任务不会出现没有操作的试次                 |
| 操作反馈 | `feedbackSound`        | `bool` | 1      | 控制正式测评时候是（1）否（0）给出声音反馈       |
| 操作反馈 | `feedbackVisual`       | `bool` | 1      | 控制正式测评时候是（1）否（0）给出声音反馈       |

# 数据记录

* 题目结果显示
    + 显示获得的奖励分数和成功兑换奖励的气球个数
    + 展示标准分数和超越百分比
* 提交原始数据变量表（统一使用`;`分割不同试次；使用`,`分割单个试次中记录的不同变量。）

| 名称         | 类型   | 说明                   |
|--------------|--------|------------------------|
| `balloon`    | `int`  | 气球编号，即第几个气球 |
| `NHit`       | `int`  | 打气次数               |
| `Feedback`   | `bool` | 气球是否爆炸           |
| `CurUtility` | `int`  | 当前气球的奖励         |
| `CumUtility` | `int`  | 累计奖励               |

# 试题算分

* 算分码：`r SDK`
* 传入参数：

| 参数名称 | 类型        | 说明                     |
|----------|-------------|--------------------------|
| `hits`   | `int array` | 所有未爆炸气球的打气次数 |

# 修订历史
