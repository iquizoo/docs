---
title: 速算大师（balloon）
author: 张亮
date: '2018-04-01'
slug: balloon
code: 78
tasknames: 数学计算
bibliography: refs/MyFullResearch.bib
link-citations: yes
output:
  blogdown::html_page:
    toc: yes
    md_extensions: -ascii_identifiers
---

```{r basic settings, include=FALSE}
NAME_CN <- "速算大师"
NAME_EN <- "balloon"
CODE <- "78"
SDK <- 104
VER <- "C2游戏"
```

## 基本信息

* **显示名称**：`r NAME_CN`
* **英文名称**：`r NAME_EN`
* **题目编号**：`r CODE`
* **任务描述**：根据算术法则快速计算，训练更新加工信息的能力。
* **相关任务**：速算师（交互式题目）、速算大师（C2游戏）。

## 交互界面

以现有的C2游戏**速算大师**为准。需作如下**调整**：

1. 修改气球大小适配两位数的显示。
1. 修改选项数目为四个。
1. 增加一个明显标志说明目前是做加法还是减法。
1. 增加计分牌，显示目前得了多少分。（对一次就加一分，错一次就减一分）。

## 详细说明

**数字**。使用**两位数**做素材。

**关卡**。只做3个关卡，用户依次完成。第一个关卡只做两位数的**加法**，第二个关卡只做两位数的**减法**，第三个关卡则按照“**加法-减法-加法-减法-...**”的顺序来做。

**题库配置**。本题目提供一个表格文件，其中包含了所有可能的题目组合。表\@ref(tab:stimuli)给出了随机取出10个题目组合，其中：

* `left`
: 左侧数字
* `right`
: 右侧数字
* `type`
: 计算法则。`add`：加法，`minus`：减法。
* `level`
: 计算难度。`easy`：简单，`medium`：中等，`difficult`：困难。
* `correct`, `opt1`, `opt2`, `opt3`
: 备选选项。注意`correct`是正确答案，其他三个都是混淆的错误选项。实际呈现的时候，四个选项的位置是随机的。

```{r stimuli, echo=FALSE, message=FALSE, warning=FALSE}
knitr::kable(readr::read_csv(here::here("data", "config", NAME_EN, "stimuli.csv")),
             caption = "题目组合样例")
```

## 数据记录

* 题目结果显示
    + 显示正确率和平均正确反应时间
    + 展示标准分数和超越百分比
* 提交原始数据如下表（请参考[标准](`r blogdown::shortcode("relref", "standard.html")`)）

```{r recording-variables, echo=FALSE, message=FALSE, warning=FALSE}
knitr::kable(readr::read_csv(here::here("data", "config", NAME_EN, "recs.csv")),
             caption = "原始数据变量列表")
```
